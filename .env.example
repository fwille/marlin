# Copy this file to .env.local and fill in your own values.
# .env.local is gitignored and never committed.

# Free IUCN Red List API v4 token — register at https://api.iucnredlist.org
# Without this, conservation status falls back to iNaturalist data only (lower coverage).
EXPO_PUBLIC_IUCN_TOKEN=
